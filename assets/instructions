The image in assets is an example file, you can safely delete it. Replace it with your sprite sheet and rename it to anim.png to work. Currently, only png files are accepted. You can change the file type in the code. You can also change what the output animation file is called in the code.